    /**
     * appi.isDate
     *
     * @description
     *
     * Verifica se um objeto/variável é do tipo Date.
     *
     * @param varObject Qualquer identificador de variável
     */
    function isDate(varObject) {
        return (varObject instanceof Date);
    }