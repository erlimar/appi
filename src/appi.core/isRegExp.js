    /**
     * appi.isRegExp
     *
     * @description
     *
     * Verifica se um objeto/variável é do tipo RegExp.
     *
     * @param varObject Qualquer identificador de variável
     */
    function isRegExp(varObject) {
        return (varObject instanceof RegExp);
    };